[build]
  command = "npm run build && mkdir -p .netlify/static && cp -r .output/public/. .netlify/static/ 2>/dev/null || true"
  publish = ".netlify/static"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from   = "/*"
  to     = "/.netlify/functions/server"
  status = 200
